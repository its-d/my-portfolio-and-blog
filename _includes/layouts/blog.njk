---
# Blog post layout: summary, metadata, hero image, share, About Author, Giscus
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  {% if baseUrl %}<base href="{{ baseUrl }}/">{% endif %}
  <title>{{ title }} | {{ site.name }}</title>
  <link rel="stylesheet" href="css/style.css">
  <link rel="icon" href="images/favicon.ico" type="image/x-icon">
  <link rel="icon" href="images/favicon-32x32.png" type="image/png" sizes="32x32">
  <link rel="apple-touch-icon" href="images/apple-touch-icon.png" sizes="180x180">
  {% if description %}
  <meta name="description" content="{{ description }}">
  {% endif %}
  <meta property="og:title" content="{{ title }}">
  {% if description %}
  <meta property="og:description" content="{{ description }}">
  {% endif %}
</head>
<body>
  {% include "partials/nav.njk" %}
  <main class="blog-main">
    <article class="blog-article">
      <header class="blog-header">
        <h1 class="blog-title">{{ title }}</h1>
        {% if summary %}
        <p class="blog-summary">{{ summary }}</p>
        {% endif %}
        <div class="blog-meta">
          <span class="blog-meta-by">Posted by: <a href="{{ site.github }}" target="_blank" rel="noopener noreferrer">{{ site.authorName }}</a> on {% if date %}<time datetime="{{ date | date('YYYY-MM-DD') }}">{{ date | date('MMM d, yyyy') }}</time>{% endif %}</span>
          {% if categories and categories.length %}
          <span class="blog-meta-cats">Category: {% for cat in categories %}<a href="{{ '/blog/' | url }}?category={{ cat | urlencode }}" class="blog-cat-tag">{{ cat | categoryLabel }}</a>{% if not loop.last %} {% endif %}{% endfor %}</span>
          {% endif %}
        </div>
        {% if image %}
        <div class="blog-hero-image-wrap">
          <img src="{{ image }}" alt="" class="blog-hero-image" width="800" height="420">
        </div>
        {% endif %}
      </header>
      <div class="blog-body">
        {{ content | safe }}
      </div>
      <footer class="blog-footer">
        <div class="blog-share">
          <span class="blog-share-label">Share this on</span>
          <a href="https://twitter.com/intent/tweet?text={{ title | urlencode }}&url={{ page.url | url | absoluteUrl }}" target="_blank" rel="noopener noreferrer" class="blog-share-link">X</a>
          <a href="https://www.linkedin.com/sharing/share-offsite/?url={{ page.url | url | absoluteUrl }}" target="_blank" rel="noopener noreferrer" class="blog-share-link">LinkedIn</a>
        </div>
        <div class="blog-about-author">
          <h3 class="blog-about-title">About {{ site.authorFullName }}</h3>
          <div class="blog-about-inner">
            <img src="images/hero-profile.png" alt="{{ site.authorFullName }}" class="blog-about-photo" width="80" height="80">
            <div class="blog-about-text">
              <p>{{ site.authorBioShort }}</p>
              <a href="{{ site.github }}" target="_blank" rel="noopener noreferrer" aria-label="GitHub">GitHub</a>
              <a href="{{ site.linkedIn }}" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn">LinkedIn</a>
            </div>
          </div>
        </div>
        <div class="blog-comments" id="comments">
          <p class="blog-comments-label">Comments</p>
          {% include "partials/giscus.njk" %}
        </div>
      </footer>
    </article>
  </main>
  {% include "partials/footer.njk" %}
  <script src="js/main.js"></script>
</body>
</html>
